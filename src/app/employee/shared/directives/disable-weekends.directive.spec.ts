import { DisableWeekendsDirective } from './disable-weekends.directive';

describe('DisableWeekendsDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableWeekendsDirective();
    expect(directive).toBeTruthy();
  });
});
